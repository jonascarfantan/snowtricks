<header class="h-12">
    <nav class="grid grid-cols-12 text-lg h-8 pt-3">
        <div class="col-span-3 pl-8">
            <a href="{{ path('home') }}" class="font-snowtricks font-black italic text-3xl ">SNOWTRICKS</a>
        </div>
        <div class="col-span-7">
            <div class="">
                <ul class="text-right">
                    <li class="inline-block mr-4">
                        <a href="/">Tricks</a></li>
                    <li class="inline-block mr-4">
                        <a href="/">Accueil</a></li>
                    <li class="inline-block mr-4">
                        <a href="/">Sponsor</a></li>
                </ul>
            </div>
        </div>
        <div class="col-span-2 pr-8">
            <nav class="class">
                <ul class="class text-right">
                    {% if app.user %}
                        <li class="inline-block mr-4">
                            <a href="{{ path('logout') }}">déconnexion</a>
                        </li>
                    {% else %}
                        <li class="inline-block mr-4">
                            <a href="{{ path('login') }}">connexion</a>
                        </li>
                        <li class="inline-block mr-4">
                            <a href="{{ path('register') }}" class="register_link">inscription</a>
                        </li>
                    {% endif %}
                </ul>
            </nav>
        </div>
    </nav>
</header>
{% block javascripts %}
    <script>

    </script>
{% endblock %}
