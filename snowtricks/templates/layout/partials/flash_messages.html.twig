{% for type, messages in app.flashes %}
	{% for message in messages %}
		<div class="fixed top-0 right-0 m-6">
			<div class="bg-red-200 text-red-900"
{#					:class="{#}
{#					        'bg-red-200 text-red-900': {% if type == 'error' %}true{% else %}false{% endif %},#}
{#					        'bg-green-200 text-green-900': {% if type == 'success' %}true{% else %}false{% endif %},#}
{#					      }"#}
					style="min-width: 240px"
			>
				<button class="opacity-75 cursor-pointer absolute top-0 right-0 py-2 px-3 hover:opacity-100">
					×
				</button>
				<div class="flex items-center">
					{{ message }}
				</div>
			</div>
		</div>
	{% endfor %}
{% endfor %}
